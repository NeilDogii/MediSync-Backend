model ContactRequests {
    id      String  @id @default(uuid())
    name    String?
    email   String?
    phone   String?
    topic   String?
    message String?

    createdAt DateTime @default(now())
}
